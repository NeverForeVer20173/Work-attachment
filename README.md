# Work attachment

作品附件。较大的虫害目标检测.rar 被拆分为 archive-parts 中的分卷（每卷不超过 90 MiB）。

下载完整仓库后，在 Windows PowerShell 中执行：

    powershell -ExecutionPolicy Bypass -File .\restore-archive.ps1

还原后文件大小应为 7,931,076,980 字节。还原脚本不会覆盖已有文件。
